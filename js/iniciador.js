document.addEventListener("DOMContentLoaded", () => {
    
    // 1. Inicializar AOS (Animaciones al scrollear)
    AOS.init({
        once: true, // La animación solo ocurre una vez
        offset: 100,
    });

});